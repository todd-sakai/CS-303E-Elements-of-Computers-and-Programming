# Homework 4: Decision Tree

## Overview
This program uses a decision tree structure to determine if a person will purchase a computer. It takes user input for age, income, student status, and credit rating, and applies conditional logic to predict the outcome.

## Concepts Covered
* Conditional statements (`if`/`elif`/`else`)
* Type casting and user input
* Boolean logic