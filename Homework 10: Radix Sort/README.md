# Homework 10: Radix Sort

## Overview
This program generates a list of random integers and sorts them using the Radix sort algorithm. It extracts digits from least to most significant and iteratively reorders the dataset into sorting bins.

## Concepts Covered
* Sorting algorithms
* 2D lists (lists of lists)
* Modulo arithmetic for digit extraction